/** Enum representing the supported run modes for the CDS extractor. */
export enum RunMode {
  AUTOBUILD = 'autobuild',
  DEBUG_PARSER = 'debug-parser',
  DEBUG_COMPILER = 'debug-compiler',
  INDEX_FILES = 'index-files',
}
