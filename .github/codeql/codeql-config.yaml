name: "My CodeQL config"

queries:
  # for log-injection
  - uses: security-and-quality
  # for ui5 queries
  - uses: ./javascript/frameworks/**/src
  
query-filters:
- exclude:
    problem.severity:
      - note

paths:
  - "**/*.xml"
  - "**/*.json"
paths-ignore:
  - "**/frameworks/*/test/models"
